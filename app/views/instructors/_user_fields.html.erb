	<!-- fields -->
	<%= f.input :username %>
	<%= f.input :password %>
	<%= f.input :password_confirmation %>
	<% if can? :manage, User %>
	<%= f.input :role, collection: User::ROLES, :prompt => true %>
	<%= f.input :active %>
	<% end %>
