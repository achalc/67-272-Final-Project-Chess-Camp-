<h3>Active Locations</h3>

<table class="table">
	<tr>
		<th width="200">Name</th>
		<th width="200">Address</th>
		<th width="100">Capacity</th>
		<th></th>
	</tr>

	<% @active_locations.each do |location| %>
		<tr>
			<td><%= link_to location.name, location_path(location)%></td>
			<td><%= location.street_1 %></td>
			<td><%= location.max_capacity %></td>
			<% if can? :manage, Location %>
				<td><%= link_to "Edit", edit_location_path(location), class: "buttony tiny radius" %></td>
			<% end %>
		</tr>

	<% end %>
</table>
<span class="paginate">
<%= will_paginate @active_locations, :previous_label => "Previous&nbsp;", :next_label => "&nbsp;Next" %>
</span>